// tests/sanity.test.js
import { describe, it, expect } from "vitest";

describe("MyAZStore Backend Tests", () => {
  it("Vitest is working perfectly", () => {
    expect(true).toBe(true);
    expect("MyAZStore").toBe("MyAZStore");
  });

  it("Math still works", () => {
    expect(1 + 1).toBe(2);
  });
});
